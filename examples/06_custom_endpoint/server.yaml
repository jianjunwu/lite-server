server:
  http_port: 8000

model_repository:
  path: ./model_repo

# Custom endpoints directory (separate from model repository)
endpoints_dir: ./endpoints

orchestration:
  control_mode: explicit
  load_models:
    - echo
