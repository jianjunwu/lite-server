server:
  http_port: 8000

model_repository:
  path: ./model_repo

orchestration:
  control_mode: explicit
  load_models:
    - sse_tokens
    - ws_echo
    - decoupled_push
    - bidi_session
    - stream_errors
