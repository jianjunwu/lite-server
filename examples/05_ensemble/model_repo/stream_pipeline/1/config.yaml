ensemble:
  steps:
    - name: preprocess
      model: step_a
      version: "1"
      inputs:
        input: "$request.input"

    - name: caption
      model: caption_stream
      version: "1"
      stream: true        # tail streaming: the DAG output is this step's chunk stream
      inputs:
        input: "$preprocess.output"
